describe('websocket integration tests', async () =>
{
  const
  expect  = require('chai').expect,
  context = require('mochawesome/addContext')

  let server, client

  beforeEach(() =>
  {

  })

  afterEach(() =>
  {

  })

  it('websocket responding', () =>
  {

  })

  it('websocket responding with multiple responses', () =>
  {

  })
})
